#include <stdio.h>
#include "udp.h"

int main() {
    printf("Client started\n");

    // TODO:
    // 1. Create UDP socket
    // 2. Build a quic_packet_t with sample data
    // 3. Encode packet to buffer
    // 4. Send packet to server UDP address
    // 5. Cleanup and exit
    
    return 0;
}